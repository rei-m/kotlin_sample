<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.rei_m.kotlinsample" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ListSampleActivity"
            android:label="@string/title_activity_list_sample">
        </activity>
        <activity
            android:name=".activities.PagerSampleActivity"
            android:label="@string/title_activity_pager_sample">
        </activity>
        <activity
            android:name=".activities.RxSampleActivity"
            android:label="@string/title_activity_rx_sample">
        </activity>
        <activity
            android:name=".activities.RealmSampleActivity"
            android:label="@string/title_activity_realm_sample">
        </activity>
    </application>

</manifest>
